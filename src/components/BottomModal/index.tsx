import Taro, { useRouter } from '@tarojs/taro';
import { View, Button } from '@tarojs/components';

const { useState, useEffect, useMemo } = Taro;

const BottomModal = (props) => {
    
    return <View className='bottomWrap'>
        <View>

        </View>
    </View>
}

export default BottomModal;